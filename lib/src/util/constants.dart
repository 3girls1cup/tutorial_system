class Constants {
  static const Duration defaultConditionTimeout = Duration(seconds: 20);
  static const Duration repeatConditionCheckInterval =
      Duration(milliseconds: 500);
  static const Duration waitBetweenSteps = Duration(milliseconds: 0);
}
